{
    "name": "smartngon-monorepo",
    "version": "1.0.0",
    "private": true,
    "description": "Smart Ngangon - Platform Peternakan Kambing Cerdas",
    "scripts": {
        "dev": "concurrently -n \"frontend,backend\" -c \"cyan,yellow\" \"npm run dev:frontend\" \"npm run dev:backend\"",
        "dev:frontend": "cd frontend && npm run dev",
        "dev:backend": "cd backend-python && . venv/bin/activate && python main.py",
        "install:all": "npm install && cd frontend && npm install",
        "setup:backend": "cd backend-python && python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt",
        "setup": "npm run install:all && npm run setup:backend",
        "build": "cd frontend && npm run build",
        "start": "cd frontend && npm run start"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}